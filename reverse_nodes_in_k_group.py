
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


def swap(head: ListNode, k: int):
    if not head:
        return head

    






node = ListNode(1, ListNode(2, ListNode(3, ListNode(4))))
k = 2
swap(node, k)
